name: Docker Image CI

on:
  push:
    branches: [ "master" ]


jobs:

  build:
    environment: main
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker-compose up -d
    - name: Debug
      run: docker ps -a
